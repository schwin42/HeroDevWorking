using UnityEngine;
using System.Collections;
using UnityEditor;

namespace VoxelMax
{/*
    [@CustomEditor(typeof(VoxelPhysics))]
    public class VoxelPhysicsEditor : Editor
    {
        public override void OnInspectorGUI()
        {
            base.OnInspectorGUI();
            /*
            if (Application.isPlaying)
            {
                //Mode label with hotkey string
                EditorGUILayout.BeginVertical("Box");
                EditorGUILayout.LabelField("Voxel Physics editor is not available during gameplay!");
                EditorGUILayout.EndVertical();
                return;
            }
            
        }
    }*/
}
